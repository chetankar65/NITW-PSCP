#include <iostream>
using namespace std;

int main() {
    int rows, start_row, max_num, pattern_num;
    cout << "Number of rows, starting row, maximum number and number of patterns: ";
    cin >> rows >> start_row >> max_num >> pattern_num;
    
}